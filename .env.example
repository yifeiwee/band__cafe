# Database Configuration
DB_HOST=db
DB_NAME=bandcafe_db
DB_USER=bandcafe_user
# Leave the passwords below as-is to let the docker build scripts auto-generate strong values
DB_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD
DB_ROOT_PASSWORD=CHANGE_THIS_TO_A_STRONG_ROOT_PASSWORD

# Application Configuration
APP_ENV=production
APP_DEBUG=false

# Session Configuration
SESSION_LIFETIME=0
SESSION_SECURE=true
SESSION_HTTPONLY=true
SESSION_SAMESITE=Strict
